# Minimal Vale configuration (disabled by default)
StylesPath = .vale/styles
MinAlertLevel = warning

# You can add custom styles later under .vale/styles/
# For now, disable core packages to avoid noise.
Packages =

[*.md]
BasedOnStyles =
